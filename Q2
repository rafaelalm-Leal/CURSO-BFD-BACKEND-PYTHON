#Faça um Programa que leia 10 números reais e mostre-os na ordem inversa.
lista=[]
for i in range(10):
    num=int(input('Digite um número: '))
    lista.append(num)
lista.sort(reverse=True)
print('A lista em ordem inversa é:',lista)